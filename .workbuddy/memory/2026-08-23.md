# 2026-08-23

## 项目身份澄清
- 本仓库 `obsidian-vault-encrypt` 实为 **meld-cp 的 Meld Encrypt**（`obsidian-encrypt`）的 fork/检出，非原创插件。
- 用户决定将其改造为自有插件发布：id=`vault-encrypt`、name=`Vault Encrypt`、author=`hellokunzai`。

## 已完成的改造（阶段三 rebrand）
- `manifest.json`：`id`/`name`/`author`/`authorUrl` 改；`minAppVersion` 1.0.3→1.4.0；`description` 补句号；移除 `fundingUrl`（原指向 meld-cp 账号）。
- `manifest-beta.json`：同步改身份；version→`2.4.5-beta.1`；移除 fundingUrl。
- `package.json`：`name`→`vault-encrypt`、`author`→`hellokunzai`、description 补句号。
- `versions.json`：`2.4.5` 的 minAppVersion 1.0.3→1.4.0。
- `LICENSE`：保留 `Copyright (c) 2021 Cleon Pinto`，新增 `Copyright (c) 2026 hellokunzai`（MIT 衍生，必须保留上游署名）。
- `README.md`：名称/repo URL 改指 `hellokunzai/vault-encrypt`，移除原作者的 buymeacoffee/sponsor 链接。
- 构建验证：`npm ci` + `npm run build` 通过；产物 `dist/vault-encrypt-2.4.5/vault-encrypt/{main.js,manifest.json,styles.css}`；安全扫描 0 处 createContextualFragment/new Function/eval/innerHTML。
- 清理旧的 `dist/meld-encrypt-2.4.5*` 残留。

## 仍待处理（发布前阻塞项，已与用户确认中）
1. **i18n 缺失**：`src/` 无任何 i18n 机制，源码大量硬编码英文 UI 文本（setName/setDesc/setButtonText/new Notice）。skill 四大铁律要求 en+zh-cn + `t()` 包裹。需用户确认范围。
2. **打包结构**：构建产物嵌套子目录，社区发布要求三件套平铺（根层/平铺 zip）。需改 esbuild.config.mjs。
3. **docs/ 文档站**：大量引用 meld-cp 仓库/issue/文档站链接；建议保留上游变更日志归因，仅改站点指向。

## 约束
- 未做任何 git commit / 未发布（遵循死命令）。
- 本仓库与用户主 vault（`E:\workspace\obsidian\obsidian_vault`）无关，是独立 fork 改造项目。

## 追加：i18n 全量包裹 + 平铺打包完成（阶段三收尾）
- 用户确认：**完整实现 en+zh-cn**、**根目录三件套+平铺 zip**。
- 新建 `src/i18n/index.ts`：导出 `t(key, params?)`（基于 `moment.locale()`），en + zh-cn 双表，覆盖命令/ribbon/菜单/notice/设置/下拉/modal/错误，共 ~82 键；`{{param}}` 占位替换。
- 全量包裹 src 下用户可见字符串（`t()` 调用 85 处），涉及：main.ts、settings Tab、inplace encrypt/DecryptModal/PasswordModal、PluginPasswordModal、EncryptedMarkdownView、FeatureWholeNoteEncrypt、FeatureConvertNote、SessionPasswordService。
- `esbuild.config.mjs` 重写为：生产构建 main.js/styles.css 输出到仓库根 + 复制 manifest.json；阶段性平铺 `dist/vault-encrypt-2.4.5/` 后 `tar -C` 打成扁平 zip（无嵌套子目录）。
- 最终验证（`npm run build`）：tsc 0 错；zip 内容 `./main.js ./manifest.json ./styles.css`（纯净平铺，无 stale 嵌套）；安全扫描 0 处禁用 API；85 个 `t()` 键全部在表中可解析。
- **保留未改名**：命令 id（`meld-encrypt-*`）与 DOM/CSS 契约标记（`meld-encrypted-view`、`meld-encrypt-*` class）——后者是加密笔记数据格式标记，改名会破坏存量笔记解密。仅 UI 文本做 i18n。
- 仍待办（低优先级、非阻塞）：`docs/` 文档站对 meld-cp 仓库/issue 的链接引用；确认是否改命令 id 前缀（可选）。

## 阶段四反馈处理：删除左侧 ribbon 图标
- 用户截图指示左侧 ribbon 图标过多，要求全部删除。
- 移除 5 处 `addRibbonIcon` 注册：
  - `FeatureInplaceEncrypt.ts`：加密选中、光标处解密（2 个）
  - `FeatureWholeNoteEncrypt.ts`：新建加密笔记、锁定并关闭所有（2 个）
  - `FeatureConvertNote.ts`：转换加密/解密（1 个）
- 同步移除 `MarkdownView` 未使用导入（仅 ribbon 回调用到）。
- 清理 `src/i18n/index.ts` 中已废弃的 5 个 `ribbon.*` 键（en + zh-cn）。
- 重新验证：`npm run build` 通过；`src/` 中 `addRibbonIcon` 残留为 0；安全扫描 0 处禁用 API；zip 平铺三件套；`t()` 键 80/77（假阳性 `🔐`/`/`/`\n`）。
- **命令、右键菜单、设置面板功能均保留**，仅移除 ribbon 快捷入口。

## 发布前检查流水线（验证命令，备忘）
1. `npm run build` → tsc 类型检查 + esbuild 打包
2. `grep -nE "createContextualFragment|new Function\(|eval\(|innerHTML" main.js` → 安全扫描
3. `tar -tf dist/vault-encrypt-2.4.5.zip` → 确认平铺三件套
4. node 脚本提取 `t("key")` 与 i18n 表比对 → 键完整性
（重要：`t()` 缺失键不会报错，只会回退显示 key 本身，故步骤4必须人工跑）

## 追加：新增「生成随机密码」功能（阶段一→二→四）
- 用户确认需求：默认长度16/四类字符全开（可接受）；上次设置需持久化；命令+ribbon 都要。
- 升版 `2.4.5 → 2.4.6`（minor，新增功能），三文件同步（manifest/package/versions 追加 `2.4.6`→`1.4.0`）。
- 新增文件：
  - `src/features/feature-random-password/IFeatureRandomPasswordSettings.ts`：settings 接口（length/upper/lower/number/symbol）。
  - `src/features/feature-random-password/RandomPasswordModal.ts`：生成逻辑（纯 `crypto.getRandomValues`，四类池各至少1字符）+ Modal（长度输入、4 个开关、结果 textarea、重新生成/复制/完成按钮）；关闭时把设置存回插件 settings。
  - `src/features/feature-random-password/FeatureRandomPassword.ts`：注册命令 `meld-encrypt-generate-password` + ribbon 图标 `dice`。
- 改动：`main.ts` 挂载 feature + `DEFAULT_SETTINGS` 补 `featureRandomPassword` + 新增 `pluginSettings` 公开 getter（settings 原为 private）；
  `MeldEncryptPluginSettings.ts` 补 `featureRandomPassword` 字段；`i18n/index.ts` 补 en+zh-cn 键（command/ribbon.generatePassword、modal.generatePassword.*）；
  `styles.css` 补 `.meld-encrypt-random-password-result`（textarea 等宽字体 + `--font-ui-medium`，规避 1.13+ 巨字）。
- 验证：`npm run build` 通过；安全扫描 0 处禁用 API；zip 平铺三件套（`dist/vault-encrypt-2.4.6.zip`）；`t()` 键 88/89（假阳性同前）；src 硬编码用户可见字符串仅剩 2 处空 `setDesc('')` + 1 处数字 placeholder（合规）。
- 仍遵循死命令：未 git commit / 未发布。

## 追加：新增「加密/解密整个文件夹」功能（阶段一→二→四）
- 用户确认需求：触发=右键文件夹菜单+命令（作用于当前笔记所在目录）；目录选择 UI=Modal 确认目录（默认当前文件目录、可改）；递归子文件夹=保留开关、默认递归。
- **关键澄清**：用户最初想要「加密文件夹容器本身、无密码打不开文件夹」。已在阶段一说明 Obsidian 架构下不可行（无文件夹级加密原语，逐文件加密在内容保密性上等价，且不影响其他功能）。用户接受逐文件递归等效方案。
- 升版 `2.4.6 → 2.4.7`（minor），三文件同步（manifest/package/versions 追加 `2.4.7`→`1.4.0`）。
- 重构：抽取 `src/services/FileEncryptHelper.ts`（静态方法 `encryptFile`/`decryptFile`/`closeUpdateRememberPasswordThenReopen`），供单文件（`FeatureConvertNote`）与文件夹共用；`FeatureConvertNote` 删除私有重复实现、清理无用 import（`Utils`/`FileDataHelper`/`EncryptedMarkdownView`），保留 `JsonFileEncoding`（解码 hint 仍需）。
- 新增文件：
  - `src/features/feature-folder-encrypt/FeatureFolderEncrypt.ts`：注册 `folder-menu` 右键菜单（加密/解密文件夹）+ 两个命令（`meld-encrypt-folder-encrypt`/`meld-encrypt-folder-decrypt`，作用于当前文件 parent 目录）。`folder-menu` 事件在 Obsidian 类型未声明，用 `"folder-menu" as any` + `(menu:any, folder:TFolder)` 强转绕过。
  - `src/features/feature-folder-encrypt/FolderEncryptModal.ts`：选目录 + 模式（加密/解密下拉）+ 递归开关 + 运行；单密码整文件夹复用（先取 SessionPasswordService.getByFile 首文件，空则弹一次 PluginPasswordModal）；遍历 `TFolder.children` 递归收集目标扩展（`md` / `ENCRYPTED_FILE_EXTENSIONS`）；逐文件失败跳过、汇总 Notice（成功/跳过/失败）+ 失败文件列表；进度实时刷新。UI 用 `createEl`/`Setting`，无 innerHTML。
- i18n 补 en+zh-cn 键：`menu.encryptFolder`/`menu.decryptFolder`/`command.folderEncrypt`/`command.folderDecrypt`/`notice.folderNotFound`/`notice.folderNoMatchingFiles`/`notice.folderEncryptSummary`/`modal.folderEncrypt.*`（title/folder/mode/recursive/run/processing/progress/summary/failedList/done/passwordTitle）。
- 验证：`npm run build` 通过（tsc 0 错）；安全扫描 0 处禁用 API；zip 平铺三件套（`dist/vault-encrypt-2.4.7.zip`）；`t()` 键 111/116（假阳性 `🔐`/`/`/`\n`）；src 硬编码用户可见字符串仅剩 2 处空 `setDesc('')` + 1 处数字 placeholder（合规）。
- 仍遵循死命令：未 git commit / 未发布。

## 修复：新功能在设置页不可见 + 右键文件夹菜单未出现
- 用户截图反馈：设置页没有随机密码/文件夹加密区域；右键文件夹无加密/解密菜单。
- 根因 1（设置页）：`FeatureRandomPassword.buildSettingsUi()` 和 `FeatureFolderEncrypt.buildSettingsUi()` 之前为空，导致设置面板不渲染这两个功能区域。
- 根因 2（右键菜单）：最可能是测试 vault 中尚未重新加载插件（用户截图发于补设置页之前，且设置页也未显示新区域，说明加载的是旧 main.js）。
- 修复内容：
  - `FeatureRandomPassword.ts`：补 `buildSettingsUi`，含「随机密码」标题/说明 + 默认长度输入 + 大写/小写/数字/符号四个默认开关，改动即时保存。
  - `FeatureFolderEncrypt.ts`：补 `buildSettingsUi`，含「文件夹加密」标题/说明。
  - `i18n/index.ts`：en + zh-cn 补 `settings.randomPassword.*` 和 `settings.folderEncrypt.*` 键。
- 重新验证：`npm run build` 通过；`t()` 键 121/126（假阳性同前）；安全扫描、zip 平铺均通过。
- 操作提醒：必须把仓库根目录三件套（`main.js`/`manifest.json`/`styles.css`）复制到测试 vault 的 `.obsidian/plugins/vault-encrypt/`，并在 Obsidian 中执行「重新加载应用」（命令面板 → `Reload app without saving`），新设置区域与右键菜单才会生效。未做 git commit / 未发布。

## 修复：设置页补递归开关 + 右键文件夹菜单仍不出现
- 用户再次截图反馈：设置页「文件夹加密」区域只有说明、没有递归开关；右键文件夹仍无「加密文件夹/解密文件夹」。
- 根因 1（递归开关）：`FeatureFolderEncrypt.buildSettingsUi()` 之前只渲染标题和说明，未加 Toggle。已新增 `IFeatureFolderEncryptSettings.ts`（`recursive: boolean`），在 `IMeldEncryptPluginSettings` 与 `DEFAULT_SETTINGS` 中注册默认值 `true`，设置页添加「默认递归处理」开关，Modal 打开时读取该默认值。
- 根因 2（右键菜单）：代码确实已打包进 main.js（`folder-menu` 字符串与菜单项均存在），最可能原因是图标 `folder-lock`/`folder-key` 不是 Obsidian 内置图标，`setIcon` 失败导致菜单项未渲染。已将图标改为确定存在的 `lock` / `key`。
- 改动文件：
  - 新增 `src/features/feature-folder-encrypt/IFeatureFolderEncryptSettings.ts`
  - `src/settings/MeldEncryptPluginSettings.ts`：加 `featureFolderEncrypt` 字段
  - `src/main.ts`：`DEFAULT_SETTINGS` 加 `featureFolderEncrypt: { recursive: true }`
  - `src/features/feature-folder-encrypt/FeatureFolderEncrypt.ts`：图标改 `lock`/`key`；`buildSettingsUi` 加递归开关；`featureSettings` 引用
  - `src/features/feature-folder-encrypt/FolderEncryptModal.ts`：递归默认值从 settings 读取
  - `src/i18n/index.ts`：en + zh-cn 补 `settings.folderEncrypt.recursive.name/desc`
- 重新验证：`npm run build` 通过；安全扫描 0 处禁用 API；zip 平铺三件套（`dist/vault-encrypt-2.4.7.zip`）；`t()` 键 123/128（假阳性 `🔐`/`/`/`
`）；src 硬编码用户可见字符串仅剩 2 处空 `setDesc('')` + 2 处数字 placeholder（合规）。
- 操作提醒：
  1. 必须重新复制三件套到测试 vault 并执行 `Reload app without saving`。
  2. 右键文件夹后若仍无菜单，请打开 Obsidian 控制台（Ctrl+Shift+I）看是否有 `setIcon` 报错，并反馈给我；届时我会再提供命令/ribbon 备用入口。
- 未做 git commit / 未发布。

## 追加：阅读视图右键编辑体验迭代（10轮改动汇总）
- 用户多次截图反馈，对 Inplace Encrypt 的阅读视图交互做逐步打磨，全部累计：

### 1. 右键菜单加「加密选中内容」
- `FeatureInplaceEncrypt.ts` 末尾 `registerEvent(workspace.on('editor-menu', ...))`，挂顶级菜单项，紧跟 Obsidian 内置「插入」段。
- `somethingSelected()` 时启用，否则灰色。
- 复用 `this.processEncryptCommand(false, editor)`，不重复造逻辑。
- i18n 加 `menu.encryptSelection`（en + zh-cn）。

### 2. 右键菜单加「解密选中内容」+ 修阅读视图点击失效
- 右键菜单加 `menu.decryptSelection`（`lock-keyhole-open` 图标），位置与加密项对称。
- 启用条件用 `processDecryptCommand(true, editor)` 返回值判断（比 `somethingSelected()` 更准：只在选区处于加密块 / 光标停在加密块时启用）。
- **修 bug**：上一轮把密文包进 `.meld-encrypt-inline-cipher` 但 `processEncryptedCodeBlockProcessor` 还在查旧的 `.meld-encrypt-inline-reading-marker`，导致阅读视图下点击加密块无法解密。选择器改为新 class，点击事件改用 `targetEl.closest('.meld-encrypt-inline-cipher')`。

### 3. 阅读视图显示优化：「🔐加密内容🔐」占位符
- 把单次点击即弹框改成**双击查看 + 右键取消加密**两条路径。
- 渲染时 span 可见文字从完整 base64 密文改为占位符，真密文仅存 `data-meld-encrypt-encrypted` 属性。
- 后期占位符文字改为 `🔐双击查看加密内容🔐`（更明确引导）。

### 4. 右键取消加密写回文件
- 阅读视图右键「解密选中内容」行为改为：解密成功后**直接 `vault.process` 把密文块用明文替换**（不再弹 DecryptModal），弹「🔓 Note was decrypted」提示。
- 不做重加密——该段不再加密，用户手动重新选中右键加密即可。

### 5. 根因修复：取消加密不生效
- 上一轮写回文件用的是简化 `fullMarker = 🔐<base64>🔐`，丢掉了真正的 prefix（`%%🔐β ` 等）和 suffix（` 🔐%%`），跟文件原文对不上 → `split/join` 0 替换。
- 修复：`replaceMarkersRecursive` 不再 regex 切片，改成**对 DOM 文本做 prefix+suffix 全文扫描**得到原 1:1 子串，存进 `data-meld-encrypt-encrypted`。prefix 列表：'%%🔐β '/'🔐β '/'%%🔐α '/'🔐α '/'%%🔐🔐 '/🔐 '；suffix 列表：' 🔐%%'/' 🔐'。

### 6. DecryptModal 「保存」改「修改」
- i18n `modal.save`：en `Save`→`Modify`，zh `保存`→`修改`。
- 按钮逻辑不变：用原密码重新加密用户改后明文并写回文件（密码不变，仅更名）。

### 7. **修复「修改」按钮实际无效（本次）**
- 根因：`showDecryptedResultForPassword` 的 modal.onClose 直接 `resolve(true)`，**根本没处理 `save = true` 分支**，所以点「修改」modal 关了但文件没改动。
- 修复：
  - `handleReadingIndicatorClick(sourcePath, decryptable, fullMarker?)`：新增 fullMarker 参数透传；dblclick 处把 `encryptedText`（cipherEl.dataset）传进去。
  - `showDecryptedResultForPassword(sourcePath, decryptable, pw, fullMarker?)`：modal.onClose 里 if (decryptModal.save && fullMarker != null && sourcePath != null) → `crypto.encryptToBase64(decryptModal.text, pw)` → `encodeEncryption(..., decryptable.hint, decryptable.showInReadingView)` → `vault.process(file).split(fullMarker).join(reEncoded)` → `Notice(noteEncrypted)`；catch 里 `Notice(encryptionFailed)`。
  - `showDecryptedTextIfPasswordKnown` 透传 fullMarker。
  - i18n 新增 `notice.encryptionFailed`（en + zh-cn）。
- 行为：阅读视图双击 → 「🔓」弹窗 → textarea 里改明文 → 点「修改」→ 笔记里密文块被新内容的新密文替换，弹「🔐 笔记已加密 🔐」。
- 验证：`npm run build` Exit 0；安全扫描 0；`notice.noteEncrypted`/`notice.encryptionFailed`/`.save`/`onClose` 全部入 bundle。
- 仍遵循死命令：未 git commit / 未发布。
